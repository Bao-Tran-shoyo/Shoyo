import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { FaFacebook, FaLinkedin, FaEnvelope, FaPhone } from "react-icons/fa";

const InfoCard = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-gray-100">
      <Card className="w-96 p-6 text-center shadow-lg rounded-2xl bg-white">
        <img
          src="https://via.placeholder.com/150"
          alt="Avatar"
          className="w-24 h-24 mx-auto rounded-full border-4 border-blue-500"
        />
        <h2 className="text-2xl font-bold mt-4">Tên của bạn</h2>
        <p className="text-gray-600">Chức danh / Công việc</p>
        
        <CardContent className="mt-4 space-y-3">
          <Button variant="outline" className="w-full flex items-center gap-2">
            <FaPhone /> 0123-456-789
          </Button>
          <Button variant="outline" className="w-full flex items-center gap-2">
            <FaEnvelope /> email@example.com
          </Button>
          <Button variant="outline" className="w-full flex items-center gap-2">
            <FaFacebook /> Facebook
          </Button>
          <Button variant="outline" className="w-full flex items-center gap-2">
            <FaLinkedin /> LinkedIn
          </Button>
        </CardContent>
      </Card>
    </div>
  );
};

export default InfoCard;
